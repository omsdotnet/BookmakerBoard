import * as React from 'react';
import * as ReactDOM from 'react-dom';
import 'semantic-ui-css/semantic.min.css';

import { Button } from 'semantic-ui-react';

ReactDOM.render(
  <Button>Whose button is this?</Button>,
  document.getElementById('root')
);
